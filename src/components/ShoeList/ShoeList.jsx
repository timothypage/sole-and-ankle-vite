import classnames from 'classnames'

import styles from "./ShoeList.module.css"

const ShoeList = ({ className }) => (
  <div className={classnames(className)}>ShoeList</div>
)

export default ShoeList
